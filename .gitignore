.env

**/.claude/settings.local.json
.mcp.json

# Binaries (files only, not directories)
/nlm
/nlm_test
/nlm_new
/cmd/nlm/nlm
/cmd/nlm/nlm_test
*.test

# Coverage files
coverage.html
coverage.out

# HAR/JSONL captures (contain auth cookies and session tokens)
docs/captures/

# Scratch files
foo
output.har.jsonl
screenshot-*.png

# Browser data (should never be committed)
Default/
Local State
Cookies
Login Data
Web Data
*.db
*.db-*
