# NotebookLM proto migration ledger

This ledger records migration state from the live checkout. A family advances
only when each proof column has direct fixture or corpus evidence; a generated
binding alone is not proof.

The test-only `assertEquivalent` harness recursively reports phase, exact
field/index path, old value, new value, and mismatch count. It is strict by
default; family tests must perform any documented normalization before calling
it.

| RPC family | Wire proof | Semantic proof | Public API proof | CLI proof | Encoder proof | Live switch | Legacy deletion |
| --- | --- | --- | --- | --- | --- | --- | --- |
| Notes (`cFji9`, `CYK0Xb`, `cYAfTb`, `AH0mwd`) | **pass (current corpus)**: generated `GetNotesWireResponse` decodes all 8 non-empty cFji9 corpus responses (plus the keyed fixture) | **pass (full observed cFji9 corpus)**: `TestGetNotesProtoAdapterCorpusEquivalence` compares note ID, content, title, rich-text projection, and keyed tombstones against the legacy parser | **pass (unit + corpus)**: nil response, empty-entry, all captured GetNotes variants, and generated CLI projection fixture preserve ordering and fields; live RPC uses generated response | **pass (fixture)**: `TestNoteListRecordsFixture` pins JSON note-list projection, rich-text preference, fallback, normalization, truncation, and nil handling | pass: generated encoders are exercised by existing note encoder tests | **GetNotes switched**; create/mutate/delete were already generated | retained as test oracle: `parseNotesResponse` remains available for complete-corpus equivalence and future wire drift checks |
| Projects | **request proof pass** for `wXbhsf`; response corpus remains partial | unverified for the broader project family | **ListRecentlyViewedProjects behavior preserved**; other project methods mixed | unverified | **pass (live regression)**: `wXbhsf` encoder pins `[null, 1, null, [2]]` | **ListRecentlyViewedProjects uses generated encoder**; other project methods mixed | retained: project mutations and complete response corpus still need family-level proof |
| Sources and freshness | partial (typed freshness path; corpus has `hizoJc` 2/2, `izAoDd` 1/1, `b7Wfje` request 1/1 with empty response) | **pass (observed corpus identities)**: `TestLoadSourceTextProtoAdapterCorpusEquivalence` matches every hizoJc text fragment; `TestAddSourcesProtoAdapterCorpusProjection` matches izAoDd source IDs/titles; rich AddSources fields remain intentionally unprojected | **live proto path** for freshness/refresh, LoadSource, and AddSources; `LoadSourceText` uses generated decode with positional fallback | pending | **pass**: generated freshness, LoadSource, AddSources, and DeleteSources encoders are used by live methods; DeleteSources wire JSON is fixture-pinned | **freshness, refresh, LoadSource, LoadSourceText, AddSources, and DeleteSources switched** | deleted: unused positional freshness/refresh parsers had no live callers; `LoadSourceText` positional parser remains a fallback/oracle; AddSources rich-content fields remain category-(c) pending public projection proof |
| Source guides (`tr032e`) | **fixture pass**: generated response decodes the keyed guide tuple | **pass (fixture)**: `sourceGuideFromProto` matches the legacy summary/topic projection | **pass (unit)**: empty and populated projections preserve public fields | pending | **pass (unit)**: generated request encoder matches the fixed context sentinel | **GenerateSourceGuide switched** | retained: legacy positional helper remains in tests as the comparison oracle |
| Labels | **fixture pass**: populated/empty reads and `[null, labels]` mutation responses decode through `beprotojson` | **pass (fixture)**: read and mutation adapters use the typed equivalence harness | **pass (unit)**: ordering, empty slices, and public `Label` projection preserved | compile-covered; CLI label commands still need output fixtures | **pass (fixture)**: GetLabels/CreateLabel/MutateLabelsMode/MutateLabel/DeleteLabels encoders match captured shapes | **GetLabels, CreateLabel, mode 0/1, rename, source attach, deletion switched**; GenerateLabels and emoji remain legacy | retained: `parseLabelsResponse` remains the oracle for empty-mode and emoji variants |
| Artifacts | **request/response pass (corpus)**: generated gArtLc request and response projection agree with the legacy parser on the observed artifact fields; URL-derived READY override is explicitly preserved | **pass (fixture + corpus sample)**: generated and legacy projections agree on IDs/types/source counts and the four rendered slide states after URL override | generated response adapter preserves the legacy public projection; polymorphic preview fields remain outside the public adapter | **compile pass**: generated artifact types are consumed by `getArtifact`; output fixture equivalence for rich previews remains pending | **pass (fixture)**: generated ListArtifacts encoder pins the full corpus request | **ListArtifacts request and response switched**; other artifact RPCs remain mixed | retained: legacy parser remains the comparison oracle until complete-corpus rich-preview and deletion gates pass |
| Notebook guides | **fixture pass**: typed response path retains the existing `VfAZjd` wire decoder | **pass (unit)**: generated response is returned unchanged | **pass (existing record tests)**: public response and errors are unchanged | compile-covered | **pass (unit)**: generated request encoder matches the fixed context sentinel | **GenerateNotebookGuide switched** | retained: legacy custom encoder remains only in the internal-method compatibility tests |
| Sharing | **fixture pass**: `ProjectDetails` wire fixture decodes through `beprotojson`; corpus has `te3DCe` CopyProject 1/1 and `s0tc2d` MutateProject 2/2 | **pass (fixture)**: `TestProjectDetailsProtoAdapterMatchesLegacyParser`; mutation responses are full project tuples and need projection comparison | **pass (unit)**: public `OwnerName`/`IsPublic` projection and flag fallback covered; mutations unchanged | pending | pass: generated `GetProjectDetails` encoder is used by the live method | **GetProjectDetails switched**; sharing mutations remain legacy/proto mixed | retained: `te3DCe` is a small status-only candidate; `s0tc2d` is category-(c), because full project mutation responses and URL/UUID normalization exceed the current public adapter |
| Account state | **pass (corpus)**: generated `Account` decodes the compact ZwVcOc status payload and quota fields; captured request is lossless | **pass (fixture)**: `parseAccountStatusProto` matches the legacy `AccountStatus` projection | **pass (unit)**: public quota/tier values are unchanged; compact error handling remains covered by legacy tests | pending | **pass (corpus)**: generated GetOrCreateAccount encoder pins the context/surface/capability envelope | **GetAccountStatus response and request switched; GetOrCreateAccount uses the captured generated request** | retained: `parseAccountStatus` remains the legacy oracle until complete corpus and CLI gates pass |
| Audio/video results | **request pass**: GetAudioFormats sentinel, R7cb6c audio/video/slide envelopes, and gArtLc artifact filter match captured corpus shapes; result variants still need typed comparison | **video and audio listing projection pass (fixture)**; slide result adapter remains unverified | **CreateVideoOverview and default CreateAudioOverview/CreateSlideDeck project generated artifact IDs; ListAudioOverviews uses the generated artifact projection** | pending | **pass (corpus/fixture)**: generated GetAudioFormats, CreateUniversalArtifact, and ListArtifacts encoders pin captured shapes | **GetAudioFormats, default no-instructions audio/video/slide creation, and ListAudioOverviews request/response switched**; custom-instruction paths remain mixed | retained: audio instruction placement, slide custom-instruction field, and polymorphic result parsers need corpus-backed adapters |
| Analytics | **pass (fixture)**: `ProjectAnalytics` models the observed metric-series wrappers; `TestProjectAnalyticsProtoAdapterMatchesLegacyParser` proves the public projection | pass (fixture): all metric IDs, timestamps, and values match `parseProjectAnalytics` | generated `GetProjectAnalytics` request and response | pending | pass: generated encoder pins the timestamped corpus shape | **switched**; `parseProjectAnalytics` retained as oracle | retained-as-oracle pending wider corpus coverage |
| Conversation history | **pass (current corpus)**: generated `GetConversationHistoryResponse` decodes all 5 non-empty khqZz captures | **pass (full corpus)**: `TestConversationHistoryProtoAdapterCorpusEquivalence` compares full public `ChatMessage` projection, including user text and assistant rich-content text | **generated request/response path preserves message IDs, roles, ordering, and content** | pass (focused + corpus) | **pass (corpus shape)**: `TestGetConversationHistoryRequestEncoder` pins the observed context envelope, conversation ID, and limit | **GetConversations, GetConversationHistory, and DeleteChatHistory switched** | retained: `parseConversationHistoryLegacy` remains the corpus comparison oracle |
| Deep research | **`QA9ei` has one request/response capture, but generated legacy-shape bytes do not match its web context envelope; generated `e3bVqc` session model is lossless on observed fields** | **pass (full corpus)**: `TestDeepResearchGeneratedCorpusEquivalence` compares generated and legacy public projections across all 24 e3bVqc responses, covering both fast and deep modes, heterogeneous arities, report markdown, source rows, and research IDs | **e3bVqc session reads now use the generated decoder; the positional parser remains the equivalence oracle; StartDeepResearch request and opaque response behavior unchanged** | unverified | **legacy-shape pass**: `TestStartDeepResearchEncoderLegacyShape` pins the existing `[null,[1],[query,1],5,project_id]`; the captured web variant is `[ctx,null,[query,1],5,project_id]` and needs live validation before adoption; DeleteDeepResearch encoder matches LBwxtb | **e3bVqc session reads switched to generated projection; DeleteDeepResearch switched; StartDeepResearch request remains legacy-shape** | retained: `QA9ei` request envelope mismatch is an explicit wire blocker; its response is an opaque token envelope (category-(b)); `parseDeepResearchSessions` remains as a test oracle for the generated e3bVqc adapter |
| Source text | **pass (hizoJc corpus)**: generated `LoadSourceResponse` decodes both captured responses | **pass (full observed corpus)**: generated `LoadSourceText` projection matches the positional decoder for source ID/title and all text fragments | **LoadSourceText uses generated decode and falls back to the positional parser when the generated envelope is not decodable** | pass (corpus) | **pass (corpus shape)**: `TestLoadSourceRequestEncoder` pins `[source],[2],ctx` | **LoadSource and LoadSourceText switched to generated decode** | retained: `decodeLoadSourceText` remains a fallback and comparison oracle for future rich-content variants |
| Streaming chat | transport framing remains explicit; laWbsf request/response frames decode losslessly; transport-empty frames remain distinct | **pass (full corpus)**: `TestExtractChatPayloadCorpusEquivalence` compares generated answer/citation projection with the legacy extractor across all 231 real payload frames from three responses, including mapping slots without grounding metadata; citation fan-out, source-ID resolution, phase, and follow-ups are exact | generated text/citation projection preserves public values; the remaining parser owns length-prefixed transport framing, cumulative-delta emission, and callback scheduling | partial | **pass (fixture)**: generated `GenerateFreeFormStreamedWire` encoder pins source nesting, options, conversation, notebook, and sequence fields | **request construction and text/citation response projection switched**; framing parser remains the transport adapter | retained explicitly: length-prefixed framing and callback scheduling are transport concerns; generated payload decoding and public projection are corpus-equivalent |

## Notes-family evidence

- The public method `Client.GetNotes` calls the generated service binding and
  adapts `GetNotesWireResponse` through `notesFromWireResponse`.
- The adapter intentionally projects only the fields exposed by the old
  positional parser (`note_id`, `content_text`, `title`, and `rich_text`). This
  preserves public behavior while the wire model retains metadata and type
  positions for later widening with an explicit API decision.
- `parseNotesResponse` is deliberately retained as a comparison oracle. It is
  not called from normal operation and must not be deleted until observed
  payload variants, request captures, CLI behavior, and the complete corpus
  satisfy the deletion gates in the migration goal.

## Current artifact blocker

`ListArtifacts` now uses a minimal generated response adapter that preserves
the legacy public projection and promotes state to READY when a rendered
`contribution.usercontent.google.com/download` URL is present. The generated
message still exposes title and type-specific preview fields that the legacy
projection intentionally omitted; those fields are therefore not copied into
the adapter. The positional parser remains as a comparison oracle until rich
preview behavior has its own complete-corpus and deletion-gate evidence.
